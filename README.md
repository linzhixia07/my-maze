# my-maze

一个面向 6-7 岁儿童的 Web 迷宫游戏：
- 后端：Spring Boot (Java 8)
- 前端：原生 JavaScript + HTML5 Canvas
- 算法：递归回溯（Perfect Maze，连通且无环）

## 运行方式

```bash
mvn clean package
java -jar target/my-maze-1.0.0.jar
```

启动后访问：`http://localhost:8080`
